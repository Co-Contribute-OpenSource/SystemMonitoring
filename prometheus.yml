global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: "systemmonitoring"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: "fastapi"
    static_configs:
      - targets: ["fastapi:8000"]
